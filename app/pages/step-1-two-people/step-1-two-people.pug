extends ../../layouts/default

block head
  - var pageTitle = projectName + ' | step-1'

block content
  +steps(1)

  .container
    +section
      +section-header
        +section-title Параметры сеанса

      +section-content
        +parameters(2)

      +section-buttons
        +button()(href="#").__button Продолжить
