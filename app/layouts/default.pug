include _externalIncludes
include _internalIncludes

doctype html
html(lang='ru')
  include head
  body(class=(bodyClass || false))
    +header
    +burger(class=(burgerActive ? 'is-active' : false))
    +b.MAIN.main
      block content
    +footer
    script(src='assets/scripts/app.min.js')
    script(src='assets/scripts/main.js')
